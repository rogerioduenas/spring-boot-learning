spring:
  application:
    name: hello-docker
  datasource:
    url: jdbc:postgresql://db-container:5432/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true